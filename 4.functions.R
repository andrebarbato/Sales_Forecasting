# função para montar uma tabela de correlação de pearson para 2 variáveis 
# para cada loja e departamento

corr_table <- function(x, y, data){
  
}

corr_table()

n <- length(unique(train[x]))

x <- 1

           